// literal is subset of data type

let days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"]
console.log(days.toString()); //changes to string instead of array form
console.log(days);

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley' //property
    whatami: function(){ //method
        return "I am soup"
    }
};

soup.whatami();

let car = "Honda";

let dex = 10.2;